{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="panel-body">
                <table class="table table-responsive">
                    <tr>
                        <th>Email</th>
                        <td>{{ user.email }}</td>
                    </tr>
                    <tr>
                        <th>Name</th>
                        <td>{{ user.firstName }}</td>
                        <td>{{ user.lastName }}</td>
                    </tr>
                    <tr>
                        <th>Initial Cash</th>
                        <td><strong>€{{ user.initialCash }}</strong></td>
                    </tr>
                </table>
                <hr>
                <a href="{{ path("profile_edit") }}" class="btn btn-warning">Edit <span class="glyphicon glyphicon-edit"></span></a>
            </div>
        </div>
    </div>
{% endblock %}